/** @type {import('next').NextConfig} */
const nextConfig = {
  // For Static Export
  output: undefined,
  trailingSlash: true,
  images: {
    unoptimized: true,
  },
}

module.exports = nextConfig
