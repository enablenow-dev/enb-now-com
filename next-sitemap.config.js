module.exports = {
    siteUrl: process.env.SITE_URL || 'https://enablenow.com', // Replace with your site's URL
    generateRobotsTxt: true, //  Generate a robots.txt file
    sitemapSize: 7000, //  Change the size limit of each sitemap file
   
  };
  